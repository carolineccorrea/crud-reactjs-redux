

export const getCustomer = () => ({ type:{} });

export const addCustomer = (cliente) => ({ type:{}  });

export const updateCustomer = (id,cliente) => ({ type:{} });

export const deleteCustomer = (id) => ({type:{} });